#ifndef TYPES_H
#define TYPES_H

enum Type { ShortType, IntType, FloatType, DoubleType, ComplexType, BoolType };

namespace boost
{
  namespace posix_time
	{
		class ptime;
	}

	class mutex;
}

#endif
